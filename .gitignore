build
managed_components
dependencies.lock
sdkconfig.old
sdkconfig
# Per-developer ESP-IDF defaults overlay (e.g. CONFIG_TAILSCALE_AUTH_KEY)
sdkconfig.local
# Generated when running `make build-docker`
sdkconfig.docker
build-docker/

# Firmware binaries
firmware.bin
*.zip

# Release artifacts
release/

# WireGuard test keys (contain private keys — never commit)
script/wireguard/keys/

# Per-developer Makefile overrides (e.g. PROBE_HOST = your tailnet IP)
Makefile.local

# Locally-staged firmware for Web Flasher testing (CI populates these on Pages)
docs/firmware/
docs/versions.json

# Marp slide build artifacts (see components/tailscale/doc/build.js)
# (presentation.pdf is intentionally tracked as the distributable LT deck)
components/tailscale/doc/node_modules/
components/tailscale/doc/package-lock.json
components/tailscale/doc/presentation.html