build
managed_components
dependencies.lock
sdkconfig.old
sdkconfig

# Firmware binaries
firmware.bin
*.zip

# Release artifacts
release/

# WireGuard test keys (contain private keys — never commit)
script/wireguard/keys/

# Per-developer Makefile overrides (e.g. PROBE_HOST = your tailnet IP)
Makefile.local